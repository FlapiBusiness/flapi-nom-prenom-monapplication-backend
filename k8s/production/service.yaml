apiVersion: v1
kind: Service
metadata:
  name: prod-NOM PRENOM-MonApplication-backend-service
  namespace: prod-NOM PRENOM-MonApplication-backend
spec:
  ports:
    - port: 3333
      targetPort: 3333
  selector:
    app: prod-NOM PRENOM-MonApplication-backend
