apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: staging-NOM PRENOM-MonApplication-backend-ingress
  namespace: staging-NOM PRENOM-MonApplication-backend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - staging.monsousdomain.api.flapi.org
      secretName: staging-NOM PRENOM-MonApplication-backend-tls
  rules:
    - host: staging.monsousdomain.api.flapi.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: staging-NOM PRENOM-MonApplication-backend-service
                port:
                  number: 3333
