apiVersion: apps/v1
kind: Deployment
metadata:
  name: staging-NOM PRENOM-MonApplication-backend-deployment
  namespace: staging-NOM PRENOM-MonApplication-backend
  labels:
    app: staging-NOM PRENOM-MonApplication-backend
spec:
  selector:
    matchLabels:
      app: staging-NOM PRENOM-MonApplication-backend
  template:
    metadata:
      labels:
        app: staging-NOM PRENOM-MonApplication-backend
    spec:
      containers:
        - name: staging-NOM PRENOM-MonApplication-backend
          image: corentin35/NOM PRENOM-MonApplication-backend:staging-${TAG_VERSION}
          imagePullPolicy: Always
          ports:
            - containerPort: 3333
          env:
            - name: PORT
              value: '3333'
          resources:
            requests:
              cpu: '300m'
              memory: '400Mi'
            limits:
              cpu: '600m'
              memory: '800Mi'
      imagePullSecrets:
        - name: regcred
