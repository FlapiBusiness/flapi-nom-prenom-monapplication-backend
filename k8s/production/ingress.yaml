apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prod-NOM PRENOM-MonApplication-backend-ingress
  namespace: prod-NOM PRENOM-MonApplication-backend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - monsousdomain.api.flapi.org
      secretName: prod-NOM PRENOM-MonApplication-backend-tls
  rules:
    - host: monsousdomain.api.flapi.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: prod-NOM PRENOM-MonApplication-backend-service
                port:
                  number: 3333
