apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: prod-NOM PRENOM-MonApplication-backend-cert
  namespace: prod-NOM PRENOM-MonApplication-backend
spec:
  secretName: prod-NOM PRENOM-MonApplication-backend-tls
  dnsNames:
    - monsousdomain.api.flapi.org
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
