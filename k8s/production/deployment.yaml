apiVersion: apps/v1
kind: Deployment
metadata:
  name: prod-NOM PRENOM-MonApplication-backend-deployment
  namespace: prod-NOM PRENOM-MonApplication-backend
  labels:
    app: prod-NOM PRENOM-MonApplication-backend
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: prod-NOM PRENOM-MonApplication-backend
  template:
    metadata:
      labels:
        app: prod-NOM PRENOM-MonApplication-backend
    spec:
      containers:
        - name: prod-NOM PRENOM-MonApplication-backend
          image: corentin35/NOM PRENOM-MonApplication-backend:${TAG_VERSION}
          imagePullPolicy: Always
          ports:
            - containerPort: 3333
          env:
            - name: PORT
              value: '3333'
          resources:
            requests:
              cpu: '300m'
              memory: '400Mi'
            limits:
              cpu: '600m'
              memory: '800Mi'
      imagePullSecrets:
        - name: regcred
